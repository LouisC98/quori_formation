<header class="d-flex flex-row-nowrap justify-content-center p20">
    <div class="content flex-fill d-flex align-items-center flew-row-nowrap justify-content-between">
        <a href="{{ path('home') }}" class="header-logo">
            <img src="{{ asset('images/logo.png') }}" alt="logo">
        </a>
        <div class="header-search p-10 flex-fill">
            <i class="fas fa-search"></i>
            <input type="text" name="" id="">
        </div>
        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
        <div class="d-flex flex-row-nowrap align-items-center">
            <a href="{{path('current_user')}}">
                <img src="{{ app.user.image }}" alt="profile picture" class="avatar avatar-lg mr-10">
            </a>
            <small>
                <a href="{{ path('logout') }}">Déconnexion</a>
            </small>
        </div>
        {% else %}
        <div>
            <a href="{{path('signup')}}" class="btn btn-primary mr-5">Inscription</a>
            <a href="{{path('login')}}" class="btn btn-primary-connexion">Connexion</a>
        </div>
        {% endif %}
    </div>
</header>